import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({

  title: {
    fontSize: 16,
    color: '#000',
    paddingVertical: 10,
    fontWeight: '500',
  },
  input: {
    borderWidth: 1,
    borderColor: 'grey',
    borderRadius: 5,
    padding: 10,
  },
 
});

export default styles;
